# Чеклист для публикации в npm

## ✅ Подготовка завершена

### 1. Package.json настроен
- ✅ Имя пакета: `@kinescope/mcp`
- ✅ Версия: `0.1.0`
- ✅ Bin скрипт: `kinescope-mcp` → `./dist/index.js`
- ✅ Main entry: `dist/index.js`
- ✅ Types: `dist/index.d.ts`
- ✅ Files: только `dist/`, `README.md`, `LICENSE`
- ✅ Engines: Node.js >= 18.0.0
- ✅ PrepublishOnly: автоматическая сборка перед публикацией

### 2. .npmignore настроен
- ✅ Исходные файлы (`src/`) исключены
- ✅ Тестовые файлы исключены
- ✅ Документация (кроме README.md) исключена
- ✅ Только `dist/` и `README.md` включены

### 3. README.md обновлен
- ✅ Инструкции по установке
- ✅ Примеры использования
- ✅ Конфигурация для Cursor и Claude Desktop
- ✅ Описание всех опций командной строки
- ✅ Примеры использования API

### 4. Проект собран
- ✅ `npm run build` выполнен успешно
- ✅ Все файлы в `dist/` на месте
- ✅ Bin скрипт имеет правильный shebang (`#!/usr/bin/env node`)

## 📦 Команды для публикации

### Первая публикация

```bash
# 1. Войдите в npm (если еще не вошли)
npm login

# 2. Проверьте содержимое пакета
npm pack --dry-run

# 3. Опубликуйте пакет
npm publish --access public
```

### Обновление версии

```bash
# 1. Обновите версию
npm version patch  # или minor, major

# 2. Публикация произойдет автоматически (если настроен prepublishOnly)
npm publish --access public
```

## 🔧 Использование после публикации

### Через npx

```bash
# Последняя версия
npx -y @kinescope/mcp@latest --api-key YOUR_KEY --client=cursor

# Конкретная версия
npx -y @kinescope/mcp@0.1.0 --api-key YOUR_KEY
```

### Конфигурация для Cursor

Добавьте в файл конфигурации Cursor (обычно `~/.cursor/mcp.json` или `~/Library/Application Support/Cursor/User/globalStorage/rooveterinaryinc.roo-cline/settings/cline_mcp_settings.json`):

```json
{
  "mcpServers": {
    "kinescope": {
      "command": "npx",
      "args": ["-y", "@kinescope/mcp@latest", "--client=cursor"],
      "env": {
        "KINESCOPE_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

## ✅ Проверка перед публикацией

- [ ] Все тесты проходят
- [ ] Проект собран (`npm run build`)
- [ ] README.md актуален
- [ ] Версия в package.json правильная
- [ ] .npmignore настроен правильно
- [ ] Bin скрипт работает (`node dist/index.js --help`)

## 📝 Примечания

- Scoped пакеты (`@kinescope/mcp`) требуют `--access public` при первой публикации
- После публикации пакет будет доступен через `npx -y @kinescope/mcp@latest`
- Версионирование следует semantic versioning (semver)

